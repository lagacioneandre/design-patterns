Imagine um **Sistema de Conversão de Arquivos**.
1.  **Subsistemas:** `AudioExtractor`, `VideoCompressor`, `SubtitlesMerger` e `FileWriter`.
2.  **Desafio:** Crie uma classe `VideoConverterFacade` com um método `convert(filePath: string, targetFormat: string)`.