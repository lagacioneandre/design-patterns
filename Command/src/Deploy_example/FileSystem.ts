/**
 * RECEPTOR 1: Sistema de arquivos (Simulacao)
 * Responsavel por lidar com arquivos.
 */

export class FileSystem {
    copyFiles(source: string, destination: string): boolean {
        console.log(`[Sistema de arquivos] Copiando arquivos de ${source} para ${destination}...`);
        // Logica real de copia de arquivos
        return true;
    }
}